import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.eugenecard.app',
  appName: 'Eugene Card Marketplace',
  webDir: '\x16'
};

export default config;

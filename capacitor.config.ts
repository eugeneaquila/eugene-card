import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.eugenecard.app',
  appName: 'Eugene Card Marketplace',
  webDir: 'eugene-card-nine.vercel.app'
};

export default config;
